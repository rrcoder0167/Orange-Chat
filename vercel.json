{
    "version": 2,
    "builds": [
      {
        "src": "*.py",
        "use": "@vercel/python",
        "config": {
          "command": "pip install -r requirements.txt"
        }
      }
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "app.py"
      }
    ]
  }
  